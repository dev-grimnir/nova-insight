
A fresh view on the demo, after entering the passkey, throws an error about a user not being found in radius.
Snapshots fill out the rest of the day, under certain conditions.  They should only build to the time requested, if building a current day snapshot.  
geolocating customers and building a map of which customers are fed out of which remote.
