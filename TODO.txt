snapshot tooltip broken
dashboard minimizes when esc is pressed on open modal
keyboard cursor needs to be on radius username by default on add customer view
refine the appearance of the snapshot view.  it's not using space efficiently.
when maximized, the bottom of the dashboard should be slightly higher. it becomes tricky to edit a customer, with the dashboard up.
geolocating customers and building a map of which customers are fed out of which remote.
increase the visibility, when a row is moused over in the dashboard controller.
long disconnect durations should be red, not green, on the report.
