{
  "name": "nova-insight",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test": "vitest run",
    "test:watch": "vitest --watch"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vitest": "^2.0.0"
  }
}
